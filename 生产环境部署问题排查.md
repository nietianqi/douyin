# 生产环境部署问题排查指南

## 🔍 问题描述

**现象**: 浏览器请求仍然指向 `http://localhost:8080`，而不是通过 nginx 代理的 `/api`

**影响**: 前端无法正确访问后端 API

---

## ✅ 已确认正常的部分

1. ✅ nginx 配置正确（`/api/` 代理到 `http://127.0.0.1:8080/`）
2. ✅ 后端API正常（直接访问 `/api/video/recommended` 可以返回数据）
3. ✅ 代码修复已完成并推送到 GitHub

---

## 🎯 解决方案

### 步骤 1：在本地拉取最新代码

```bash
cd F:\douyin\douyin_frontend  # 你的本地路径
git pull origin claude/design-java-backend-5SUIM
```

### 步骤 2：验证配置是否正确

**检查 `src/config/index.ts`**：

```bash
# 在 Git Bash 或 PowerShell 中执行
cat src/config/index.ts | grep -A 8 "BASE_URL_MAP"
```

**应该看到**：
```typescript
const BASE_URL_MAP = {
  DEV: 'http://localhost:8080',
  PROD: '/api',  // ← 这里必须是 /api
  GP_PAGES: '',
  GITEE_PAGES: '/douyin',
  UNI: 'https://dy.ttentau.top'
}
```

**如果不是 `/api`，手动修改**：
```typescript
PROD: '/api',  // 改成这样
```

### 步骤 3：验证 request.ts 配置

**检查 `src/utils/request.ts`**：

```bash
cat src/utils/request.ts | grep -A 3 "import"
```

**应该看到**：
```typescript
import { BASE_URL } from '@/config'  // ← 必须导入 BASE_URL

export const axiosInstance = axios.create({
  baseURL: BASE_URL,  // ← 必须使用 BASE_URL
  timeout: 60000
})
```

### 步骤 4：清理并重新构建

```bash
# 1. 删除旧的构建文件
rm -rf dist
rm -rf node_modules/.vite

# 2. 确认使用生产环境构建
npm run build

# 构建时会自动加载 .env.production 文件
# .env.production 内容: VITE_ENV = "PROD"
```

### 步骤 5：验证构建结果

```bash
# 运行检查脚本
node check-build-config.js
```

**或手动检查**：
```bash
# 检查 dist 目录中的 JS 文件是否包含 localhost
grep -r "localhost:8080" dist/js/

# 如果有输出，说明构建有问题
# 如果没有输出，说明构建正确 ✅
```

### 步骤 6：上传到服务器

```bash
# 方式 A：使用 SCP
scp -r dist/* root@43.161.216.248:/var/www/douyin-vue/

# 方式 B：使用 SFTP 工具（WinSCP、FileZilla）
# 上传 dist 目录下的所有文件到 /var/www/douyin-vue/
```

### 步骤 7：服务器端操作

```bash
# SSH 登录服务器
ssh root@43.161.216.248

# 1. 设置文件权限
chown -R nginx:nginx /var/www/douyin-vue
chmod -R 755 /var/www/douyin-vue

# 2. 清除服务器缓存（如果使用了缓存）
rm -rf /var/cache/nginx/*

# 3. 重启 nginx
nginx -t  # 测试配置
systemctl reload nginx
```

### 步骤 8：浏览器端清理

在浏览器中：

1. **打开开发者工具**（F12）
2. **进入 Application 标签**
3. **清除所有存储**：
   - Local Storage
   - Session Storage
   - Cookies
   - Cache Storage

4. **强制刷新**：
   - Windows: `Ctrl + Shift + R` 或 `Ctrl + F5`
   - Mac: `Cmd + Shift + R`

5. **或使用无痕模式测试**：
   - `Ctrl + Shift + N`（Chrome）

---

## 🔍 调试步骤

### 1. 检查浏览器网络请求

打开 F12 → Network 标签，刷新页面，查看请求：

**✅ 正确的请求**：
```
Request URL: http://43.161.216.248/api/video/recommended?start=0&pageSize=10
Request Method: GET
```

**❌ 错误的请求**：
```
Request URL: http://localhost:8080/video/recommended?start=0&pageSize=10
```

### 2. 检查响应头

正确的响应头应该包含：
```
server: envoy (或 nginx)
```

### 3. 在浏览器控制台测试

```javascript
// 在浏览器控制台执行
console.log(window.location.href)  // 应该是 http://43.161.216.248/

// 检查 axios 配置（如果可以访问）
console.log(axios.defaults.baseURL)  // 应该是 /api
```

---

## 🚨 常见问题

### Q1: 构建后仍然包含 localhost

**原因**: 配置文件修改后没有保存，或使用了错误的环境变量

**解决**：
```bash
# 1. 确认修改已保存
git status
git diff src/config/index.ts

# 2. 确认 .env.production 存在
ls -la .env.production
cat .env.production  # 应显示: VITE_ENV = "PROD"

# 3. 清理后重新构建
rm -rf dist node_modules/.vite
npm run build
```

### Q2: 浏览器仍然请求 localhost

**原因**: 浏览器缓存了旧的 JS 文件

**解决**：
1. 清除浏览器所有缓存（Application → Clear storage）
2. 使用无痕模式测试
3. 在开发者工具 Network 标签勾选 "Disable cache"
4. 检查服务器上的文件修改时间是否是最新的

### Q3: nginx 返回 404

**检查 nginx 配置**：
```bash
# 测试配置
nginx -t

# 查看错误日志
tail -f /var/log/nginx/error.log

# 确认文件路径正确
ls -la /var/www/douyin-vue/
ls -la /var/www/douyin-vue/index.html
```

### Q4: API 返回 502 Bad Gateway

**检查后端服务**：
```bash
# 检查后端是否运行
netstat -tlnp | grep 8080

# 测试后端直接访问
curl http://127.0.0.1:8080/video/recommended?start=0&pageSize=10

# 查看后端日志
# (根据你的后端部署方式查看对应日志)
```

---

## 📋 完整检查清单

部署前：
- [ ] 拉取最新代码 `git pull`
- [ ] 确认 `src/config/index.ts` 中 `PROD: '/api'`
- [ ] 确认 `src/utils/request.ts` 使用 `BASE_URL`
- [ ] 确认 `.env.production` 存在且内容为 `VITE_ENV = "PROD"`
- [ ] 删除 `dist` 目录
- [ ] 执行 `npm run build`
- [ ] 验证 `dist/js/` 中不包含 `localhost:8080`

服务器端：
- [ ] 上传 `dist` 目录所有文件
- [ ] 设置正确的文件权限
- [ ] nginx 配置正确
- [ ] 重启/重载 nginx
- [ ] 后端服务正常运行

浏览器端：
- [ ] 清除所有缓存
- [ ] 强制刷新页面
- [ ] 检查 Network 请求 URL 正确
- [ ] 或使用无痕模式测试

---

## 🎯 快速诊断命令

在本地执行：
```bash
# 一键检查
node check-build-config.js
```

在服务器执行：
```bash
# 检查前端文件是否包含 localhost
grep -r "localhost:8080" /var/www/douyin-vue/js/

# 检查 nginx 配置
nginx -t && nginx -T | grep -A 10 "location /api"

# 测试 API
curl http://127.0.0.1:8080/video/recommended?start=0\&pageSize=10
curl http://43.161.216.248/api/video/recommended?start=0\&pageSize=10
```

---

## 📞 仍然有问题？

如果按照以上步骤仍然无法解决，请提供：

1. **本地构建日志**：
   ```bash
   npm run build > build.log 2>&1
   ```

2. **服务器端检查结果**：
   ```bash
   grep -r "localhost" /var/www/douyin-vue/js/ > check.log
   ls -lh /var/www/douyin-vue/js/ >> check.log
   ```

3. **浏览器网络请求截图**（F12 → Network）

4. **nginx 访问日志**：
   ```bash
   tail -100 /var/log/nginx/access.log
   ```

---

**祝部署顺利！** 🚀
