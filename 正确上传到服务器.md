# 正确上传前端文件到服务器

## 🎯 问题确认

你发现了问题：**上传的 dist 和 nginx 读取的 dist 路径不一致**

---

## 📍 第一步：确认 nginx 配置的路径

从你的 nginx 配置看：

```nginx
server {
    root   /var/www/douyin-vue;  # ← nginx 从这里读取文件
    index  index.html;
}
```

**nginx 读取路径**：`/var/www/douyin-vue`

---

## 🔍 第二步：检查服务器上的实际情况

在服务器上执行检查脚本：

```bash
# 1. SSH 登录服务器
ssh root@43.161.216.248

# 2. 检查 nginx 实际配置的路径
cat /etc/nginx/nginx.conf | grep -A 5 "server {"

# 3. 查看该目录的内容
ls -lh /var/www/douyin-vue/

# 4. 查看文件修改时间
ls -lt /var/www/douyin-vue/

# 5. 检查 JS 文件
ls -lh /var/www/douyin-vue/js/

# 6. 检查是否包含 localhost
grep -r "localhost:8080" /var/www/douyin-vue/js/
```

---

## 📤 第三步：正确上传文件

### 方式 A：使用 SCP（推荐）

在本地 Windows 的 **Git Bash** 或 **PowerShell** 中执行：

```bash
# 进入前端项目目录
cd F:\douyin\douyin_frontend

# 确保已经构建了最新的 dist
npm run build

# 上传到正确的路径（会覆盖旧文件）
scp -r dist/* root@43.161.216.248:/var/www/douyin-vue/

# 如果提示输入密码，输入: ntq123!@#
```

### 方式 B：使用 rsync（更安全，有备份）

```bash
# rsync 会同步删除服务器上多余的旧文件
rsync -avz --delete dist/ root@43.161.216.248:/var/www/douyin-vue/

# -a: 归档模式
# -v: 显示详细信息
# -z: 压缩传输
# --delete: 删除服务器上 dist/ 中不存在的文件
```

### 方式 C：先备份，再上传

```bash
# 1. 先在服务器上备份旧文件
ssh root@43.161.216.248 "cp -r /var/www/douyin-vue /var/www/douyin-vue.backup.$(date +%Y%m%d_%H%M%S)"

# 2. 清空旧文件
ssh root@43.161.216.248 "rm -rf /var/www/douyin-vue/*"

# 3. 上传新文件
scp -r dist/* root@43.161.216.248:/var/www/douyin-vue/
```

### 方式 D：使用 WinSCP（图形化界面）

1. 下载 WinSCP：https://winscp.net/
2. 连接服务器：
   - 主机：`43.161.216.248`
   - 用户名：`root`
   - 密码：`ntq123!@#`
3. 左侧选择本地的 `F:\douyin\douyin_frontend\dist\`
4. 右侧进入服务器的 `/var/www/douyin-vue/`
5. 全选左侧文件，拖到右侧（会提示覆盖，选择"全部覆盖"）

---

## 🔧 第四步：设置正确的权限

上传完成后，在服务器上执行：

```bash
# 设置所有者为 nginx
chown -R nginx:nginx /var/www/douyin-vue

# 设置权限
chmod -R 755 /var/www/douyin-vue

# 确认文件存在
ls -lh /var/www/douyin-vue/
```

---

## ✅ 第五步：验证

### 1. 检查文件是否正确上传

```bash
# 查看 index.html 是否存在
cat /var/www/douyin-vue/index.html | head -20

# 查看 JS 文件
ls /var/www/douyin-vue/js/

# 确认不包含 localhost
grep -r "localhost:8080" /var/www/douyin-vue/js/
# 应该没有输出 ✅
```

### 2. 重启 nginx

```bash
# 测试配置
nginx -t

# 重新加载
systemctl reload nginx

# 查看状态
systemctl status nginx
```

### 3. 浏览器测试

1. 清除浏览器缓存
2. 访问：http://43.161.216.248/
3. 打开开发者工具（F12）→ Network
4. 刷新页面
5. 查看请求 URL：
   - ✅ 应该是：`http://43.161.216.248/api/video/recommended`
   - ❌ 不应该是：`http://localhost:8080/video/recommended`

---

## 🚨 常见错误

### 错误 1：上传到了错误的目录

**症状**：上传成功，但网站没变化

**原因**：
- 上传到了 `/root/dist/` 而不是 `/var/www/douyin-vue/`
- 或上传到了 `/var/www/html/` 等其他目录

**解决**：
```bash
# 检查所有可能的目录
find /var /home /root -name "index.html" -path "*/dist/*" 2>/dev/null

# 确认上传到正确路径
ls /var/www/douyin-vue/index.html
```

### 错误 2：权限不足

**症状**：nginx 返回 403 Forbidden

**解决**：
```bash
# 设置正确权限
chown -R nginx:nginx /var/www/douyin-vue
chmod -R 755 /var/www/douyin-vue

# 检查 SELinux（CentOS/RHEL）
getenforce
# 如果是 Enforcing，需要设置 SELinux 上下文
chcon -R -t httpd_sys_content_t /var/www/douyin-vue
```

### 错误 3：上传了 dist 目录本身

**症状**：路径变成了 `/var/www/douyin-vue/dist/index.html`

**正确**：`/var/www/douyin-vue/index.html`
**错误**：`/var/www/douyin-vue/dist/index.html`

**解决**：
```bash
# 方式 A：移动文件
mv /var/www/douyin-vue/dist/* /var/www/douyin-vue/
rm -rf /var/www/douyin-vue/dist

# 方式 B：重新上传（注意命令中的 dist/* 而不是 dist）
scp -r dist/* root@43.161.216.248:/var/www/douyin-vue/
```

### 错误 4：旧文件未清理

**症状**：部分文件是新的，部分是旧的

**解决**：
```bash
# 先备份
cp -r /var/www/douyin-vue /var/www/douyin-vue.backup

# 清空目录
rm -rf /var/www/douyin-vue/*

# 重新上传
scp -r dist/* root@43.161.216.248:/var/www/douyin-vue/
```

---

## 📋 完整检查清单

上传前：
- [ ] 本地已拉取最新代码 `git pull`
- [ ] 本地已构建最新 dist `npm run build`
- [ ] dist 目录存在且有内容
- [ ] dist/js/ 中不包含 localhost:8080

上传时：
- [ ] 使用正确的路径 `/var/www/douyin-vue/`
- [ ] 上传的是 `dist/*`（目录内容）而不是 `dist`（目录本身）
- [ ] 确认所有文件都已上传（index.html, js/, assets/ 等）

上传后：
- [ ] 设置正确的所有者 `nginx:nginx`
- [ ] 设置正确的权限 `755`
- [ ] 重新加载 nginx
- [ ] 清除浏览器缓存
- [ ] 测试请求 URL 正确

---

## 🎯 一键上传脚本

创建这个脚本在本地使用（Windows Git Bash）：

```bash
#!/bin/bash

SERVER="43.161.216.248"
USER="root"
REMOTE_PATH="/var/www/douyin-vue"

echo "========================================="
echo "开始上传前端文件到服务器"
echo "========================================="

# 1. 检查本地 dist 目录
if [ ! -d "dist" ]; then
    echo "❌ dist 目录不存在，请先构建: npm run build"
    exit 1
fi

echo "✅ dist 目录存在"

# 2. 检查 dist 中是否有 index.html
if [ ! -f "dist/index.html" ]; then
    echo "❌ dist/index.html 不存在，请检查构建是否成功"
    exit 1
fi

echo "✅ dist/index.html 存在"

# 3. 检查是否包含 localhost
if grep -r "localhost:8080" dist/js/; then
    echo "❌ 警告：构建文件中包含 localhost:8080"
    echo "是否继续上传？(y/n)"
    read -r answer
    if [ "$answer" != "y" ]; then
        exit 1
    fi
fi

# 4. 备份服务器上的旧文件
echo ""
echo "正在备份服务器上的旧文件..."
ssh $USER@$SERVER "cp -r $REMOTE_PATH ${REMOTE_PATH}.backup.\$(date +%Y%m%d_%H%M%S)"

# 5. 上传新文件
echo ""
echo "正在上传文件..."
rsync -avz --delete dist/ $USER@$SERVER:$REMOTE_PATH/

# 6. 设置权限
echo ""
echo "正在设置权限..."
ssh $USER@$SERVER "chown -R nginx:nginx $REMOTE_PATH && chmod -R 755 $REMOTE_PATH"

# 7. 重新加载 nginx
echo ""
echo "正在重新加载 nginx..."
ssh $USER@$SERVER "nginx -t && systemctl reload nginx"

echo ""
echo "========================================="
echo "✅ 上传完成！"
echo "========================================="
echo ""
echo "请访问: http://$SERVER/"
echo "并清除浏览器缓存后测试"
echo ""
```

保存为 `upload-to-server.sh`，然后执行：

```bash
chmod +x upload-to-server.sh
./upload-to-server.sh
```

---

## 💡 调试技巧

### 快速确认文件版本

在服务器上添加版本信息：

```bash
# 在服务器上
echo "$(date '+%Y-%m-%d %H:%M:%S')" > /var/www/douyin-vue/version.txt

# 然后访问
curl http://43.161.216.248/version.txt
```

每次上传后更新这个文件，可以快速确认是否部署了最新版本。

---

**现在重新上传，应该就没问题了！** 🚀
